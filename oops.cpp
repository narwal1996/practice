#include <cstdlib>
#include <iostream>

using namespace std;



int main()
{
    int row, col;
    cin >> row >> col;
    int m[row][col];
    int n[row*col];
    for(int i = 0; i < row; i++)
        for(int j = 0; j < col; j++)
            cin >> m[i][j];
    for(int i = 0; i < row*col; i++)
        cin >> n[i];
    for(int i = 0; i < ; i++)
        printf("\n %d, a[array[i]]);
}
